#!/bin/bash
# Step-by-step Docker workflow for NDK compilation

echo "=== DOCKER NDK COMPILATION WORKFLOW ==="
echo ""
echo "📥 STEP 1: Download Android NDK from Mega into Docker"
echo "   docker run -it --rm \\"
echo "     -v ~/.config/rclone:/root/.config/rclone \\"
echo "     ndk-builder \\"
echo "     bash -c 'rclone copy Mega:/ndk/android-ndk-r25.tar.gz /ndk/ && tar -xzf /ndk/android-ndk-r25.tar.gz -C /ndk/'"
echo ""
echo ""
echo "📦 STEP 2: Compile your project with NDK inside Docker"
echo "   docker run -it --rm \\"
echo "     -v \$(pwd):/workspace \\"
echo "     -v ~/android-ndk:/ndk \\"
echo "     ndk-builder \\"
echo "     bash -c 'export NDK_HOME=/ndk/android-ndk-r25 && ./build.sh'"
echo ""
echo ""
echo "💡 Or just open an interactive shell:"
echo "   docker run -it --rm \\"
echo "     -v \$(pwd):/workspace \\"
echo "     -v ~/android-ndk:/ndk \\"
echo "     ndk-builder"
echo ""
echo "Then inside the container:"
echo "   export NDK_HOME=/ndk/android-ndk-r25"
echo "   ./your-build-command"
echo ""
